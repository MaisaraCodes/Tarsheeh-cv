Final Arabic session. Focus on polishing RTL behavior and handling edge cases.

SCOPE: Polish only. No new pages, no new features. Review the app in Arabic mode and fix any layout breaks, misaligned elements, or direction issues.

STEP 1: AUDIT

Open each Arabic page and identify layout issues. Specifically check:
- Section headers: the pattern is [number] [title] [rule]. In RTL, this should visually reverse so it reads right-to-left naturally. Does it?
- Flex rows with justify-between: do they still make sense in RTL (e.g., rank on left / score on right needs to flip)?
- Score bars: they fill from left in EN. In AR, they should fill from right. Verify the bar uses left: 0 vs right: 0 correctly.
- The processing trace panel's 5 cards: the flow reads left-to-right in EN. In AR, it should read right-to-left. Does the grid order need reversing, or is the logical-properties approach sufficient?
- The candidate ranking list: rank number | name | bar | score. In AR, this should flip to: score | bar | name | rank number.
- The upload drop zone's drag-and-drop hint text alignment
- The candidate detail page's summary row (rank/summary/score) should mirror
- Icons and arrows: back arrows, etc.
- The nav: wordmark should stay LEFT in Latin (per brand guide), language toggle/CTA on the RIGHT in EN — flipped in AR.

Produce a list of every RTL issue you find. DO NOT fix anything yet. Show me the list.

STEP 2: FIX (only after I approve the list)

Fix each issue in the list. Use Tailwind logical properties wherever possible. For cases where logical properties don't cover it (grid order, absolute positioning), use a [dir="rtl"] CSS selector in globals.css or a conditional in the component.

STEP 3: TYPOGRAPHY CHECK

Confirm Amiri loads for Arabic headings and Cairo loads for Arabic body. Compare visually to the Arabic brand guide (/tarsheeh_brand_guide_AR.html). Flag any typography mismatches.

STEP 4: TRANSLATION QUALITY CHECK

Re-read every Arabic string and compare to the brand guide. Any string that looks machine-translated (unnatural phrasing, wrong grammar) must be flagged. Do NOT attempt to fix Arabic phrasing yourself. Report flagged strings to me and I will provide corrections.

STEP 5: BUILD AND COMMIT

After all fixes are approved and applied:
- npm run build
- Test every page in both locales one more time
- Commit with message: "feat(frontend): full Arabic i18n with RTL support"
- Push to dev

Final verification output: screenshots or description of each page in both EN and AR, showing the layout is correct and translations are in place.