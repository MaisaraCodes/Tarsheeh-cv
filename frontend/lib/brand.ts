// All user-facing copy now lives in /messages/{en,ar}.json and is consumed
// via next-intl. The legacy COPY / TAGLINES / PHRASES exports were removed
// to avoid drift between the dictionary and English-only fallbacks.

export const CORNER_BRACKET_SIZE = 28;
